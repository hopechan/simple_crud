import RestController from './api/restController.js';
document.addEventListener('DOMContentLoaded', function (event) {
    console.log('I´m In');
})

function recargar() {
    const rest = new RestController(), tbody = document.getElementById('cuerpo');
    let col = [];
}